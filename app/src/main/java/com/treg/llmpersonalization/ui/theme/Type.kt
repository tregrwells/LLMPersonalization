package com.treg.llmpersonalization.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.font.FontFamily

// Tinos bundled as a resource will replace this once downloads succeed.
// For now, use the platform serif — visually closer to Times than the default.
val TinosFamily = FontFamily.Serif

val AppTypography = Typography()